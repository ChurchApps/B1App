import { ElementInterface } from "@/helpers";

interface Props {
  element: ElementInterface;
}

export const ElementBlock: React.FC<Props> = (props) => {
  let result = <>Element Block - **TODO: Implement**</>;
  return result;
};
